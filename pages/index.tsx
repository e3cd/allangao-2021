import FixedSidebar from "../components/FixedSidebar";
import Layout from "../components/Layout";
import ScrollWrapper from "../components/ScrollWrapper";

const IndexPage = () => (
  <Layout title="Allan Gao Website">
    <FixedSidebar />
    <ScrollWrapper />
  </Layout>
);

export default IndexPage;
